configs
data // 数据集目录
data/examples
docs
diffsynth // 暂时不动
models // 存放其他调用的，不属于 diffsynth 框架内的模型代码
checkpoints // 存放需要加载的模型权重
scripts // shell 脚本
tools // 工具 python 脚本，如 train 等
utils // 工具函数
results // 评估结果、测试结果等
outputs // 训练过程的输出
main.py // 入口函数，需要简单，仅做路由
requirements.txt // 依赖文件
